# Workspace build artifacts (shared target dir lives at the repo root).
/target
**/*.rs.bk

# Local SQLite databases and env files.
# Covers the DB plus its WAL sidecars (-wal/-shm) and rollback journal.
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
.env
